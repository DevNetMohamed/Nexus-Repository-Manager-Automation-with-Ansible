---
- name: Download Nexus archive (skipped if already present)
  ansible.builtin.get_url:
    url: "{{ nexus_download_url }}"
    dest: "{{ nexus_download_dest }}"
    mode: "0644"
    force: false
  when: not nexus_install.stat.exists
